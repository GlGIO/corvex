# Context Directory

Place files here that should be included in every task's context.

Examples:
- `architecture.md` — project architecture overview
- `conventions.md` — coding conventions and patterns
- `database.md` — database schema and guidelines

All `.md` files in this directory are automatically included via the
`context.always_include` config setting.
